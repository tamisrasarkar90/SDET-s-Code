/*
Problem Dewscription:A string is good if there are no repeated characters.
Given a string s​​​​​, return the number of good substrings of length three in s​​​​​​.
Note that if there are multiple occurrences of the same substring, every occurrence should be counted.A substring is a contiguous sequence of characters in a string.


Example 1:

Input: s = "xyzzaz"
Output: 1
Explanation: There are 4 substrings of size 3: "xyz", "yzz", "zza", and "zaz". 
The only good substring of length 3 is "xyz".
Example 2:

Input: s = "aababcabc"
Output: 4
Explanation: There are 7 substrings of size 3: "aab", "aba", "bab", "abc", "bca", "cab", and "abc".
The good substrings are "abc", "bca", "cab", and "abc".
 
Constraints:
1 <= s.length <= 100
s​​​​​​ consists of lowercase English letters.

*/
/*class Solution {
    public int countGoodSubstrings(String s) 
    {
        //Step 1: take all substring
        //Step 2: Keep them in one List
        //Step 3: remove the duplicate charcater one
        //First take all substring of length 3
        int count=0;
        for(int i=0;i<=s.length()-3;i++)
        {
          String st=s.substring(i,i+3);
          Set<Character> set=new HashSet();
           boolean flag=true;
           for(int k=0;k<st.length();k++)
           {
             if(set.contains(st.charAt(k)))
             {
                flag=false;
                break;
             }
             set.add(st.charAt(k));
           }
          if(flag)
          {
            count++;
          }
        }
        return count;    
    }
} 
//TC:O(N^2)
//Approach 2: TC:O(N) */

 class Solution {
    public int countGoodSubstrings(String s) 
    {
        int count=0;
        for(int i=0;i<=s.length()-3;i++)
        {
            char a=s.charAt(i);
            char b=s.charAt(i+1);
            char c=s.charAt(i+2);
            if(a!=b && b!=c && a!=c)
            {
              count++;
            }
        }
        return count;
    }
 }
 //TC:O(N)
 //SC:O(1)
